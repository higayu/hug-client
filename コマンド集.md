# 起動コマンド

## 開発環境での起動（Vite開発サーバー + Electron）
```bash
# 開発（2ターミナルで）
npm run dev
npm run electron

# ビルド確認
npm run build:renderer
npm run build:preload
npm start

# 配布用パッケージ
npm run dist
```


開発環境では、Vite開発サーバー（`http://localhost:5173`）とElectronが自動的に起動します。

## 本番環境での起動
```bash
npm run build:renderer
npm run build:preload
npm start

npm start -- --dev
```

## その他のコマンド
```bash
node puppeteer-test.js
```

# 依存関係のインストール

## 初回セットアップ時
```bash
# ルートディレクトリの依存関係をインストール
npm install

# rendererディレクトリの依存関係をインストール
cd renderer
npm install
cd ..
```


# バージョンの指定
```bash
npm version 1.0.9
```
# バージョンの自動更新
```bash
npm version patch

npm version major
```

# ビルドコマンド

## rendererのビルド（React + Vite）
```bash
npm run build:renderer
npm run build:preload
```

ビルド成果物は`renderer/dist/`に出力されます。

## ① Windows版インストーラを生成してGitHubに自動公開



# ビルドコマンド
PowerShellまたはターミナルで実行👇

## リモートリポジトリにアップロードする（windowsアプリで）

### バージョンの自動更新
```bash
npm version patch
npm version minor
npm version major
```

```bash
# rendererをビルドしてから実行

npm version patch

npm version major

npm run build:renderer
npm run build:preload

npx electron-builder --win --publish always
```

## Windows でビルド
```bash
# rendererをビルドしてから実行
npm run build:renderer
npm run build:preload
npm run dist -- --win --x64
```

## Macでビルド
```bash
# rendererをビルドしてから実行
npm run build:renderer
npm run build:preload
npm run dist -- --mac
```

## 完全なビルド（renderer + Electron）
```bash
npm run dist
```

このコマンドは自動的にrendererをビルドしてからElectronアプリをビルドします。

# プロジェクト構成（React + Vite版）

```
hug-client/
  ├─ main.js
  ├─ preload.js
  ├─ src/
  │   └─ apiClient.js
  ├─ renderer/              ← React + Viteプロジェクト
  │   ├─ index.html
  │   ├─ vite.config.js
  │   ├─ package.json
  │   ├─ src/
  │   │   ├─ main.jsx        ← Reactエントリーポイント
  │   │   ├─ App.jsx         ← Reactコンポーネント
  │   │   └─ index.css
  │   ├─ modules/            ← 既存のモジュール（そのまま使用）
  │   └─ dist/               ← ビルド成果物（本番環境用）
  ├─ _renderer/             ← バックアップ（参考用）
  └─ main/
      ├─ window.js
      └─ ipcHandlers.js
```

# 開発環境と本番環境の違い

## 開発環境
- Vite開発サーバー（`http://localhost:5173`）経由でrendererを読み込み
- ホットリロード有効
- 絶対パス・相対パスともに動作

## 本番環境
- ビルド済みファイル（`renderer/dist/index.html`）を読み込み
- 相対パスのみ動作（`base: './'`設定済み）
- webviewのpreload属性は動的に設定される


## Whisper.cppをビルドとモデルのダウンロード

### Whisper.cpp のダウンロードと モデルのダウンロード
```bash
# 作業フォルダー
$parent = "C:\Users\higayu\Documents\_hug-client"
$parent = "C:\Users\higay\Documents\_hug-client"
$parent = "C:\Users\Higashiyama\Documents\_hug-client"
```

PowerShellで以下を実行します。

```powershell
$ErrorActionPreference = "Stop"

# 作業フォルダー
# $parent = "C:\Users\Higashiyama\Documents\_hug-client"
$parent = "C:\Users\higay\Documents\_hug-client"

# 作業フォルダーを作成して移動
New-Item -ItemType Directory -Force -Path $parent | Out-Null
Set-Location $parent

# whisper.cpp の保存先
$repoPath = Join-Path $parent "whisper.cpp"

# 既存の whisper.cpp がある場合はバックアップ
if (Test-Path $repoPath) {
    $backupName = "whisper.cpp_backup_$(Get-Date -Format 'yyyyMMdd_HHmmss')"

    Rename-Item `
        -Path $repoPath `
        -NewName $backupName

    Write-Host "既存の whisper.cpp を退避しました: $backupName"
}

# whisper.cpp を新しく取得
git clone https://github.com/ggml-org/whisper.cpp.git

# whisper.cpp フォルダーへ移動
Set-Location $repoPath

# Release構成でビルド
cmake -S . -B build
cmake --build build --config Release -j

# モデル保存フォルダー
$modelDir = Join-Path $repoPath "models"

New-Item `
    -ItemType Directory `
    -Force `
    -Path $modelDir | Out-Null

# 日本語対応の多言語baseモデルをダウンロード
# 第2引数で保存先を明示する
& ".\models\download-ggml-model.cmd" base $modelDir
```

### ビルド結果とモデルの確認

```powershell
$whisperExe = Join-Path `
    $repoPath `
    "build\bin\Release\whisper-cli.exe"

$modelFile = Join-Path `
    $repoPath `
    "models\ggml-base.bin"

if (-not (Test-Path $whisperExe)) {
    throw "whisper-cli.exe が見つかりません: $whisperExe"
}

if (-not (Test-Path $modelFile)) {
    throw "モデルファイルが見つかりません: $modelFile"
}

Write-Host "ビルド成功: $whisperExe"
Write-Host "モデルのダウンロード成功: $modelFile"
```

### サンプル音声で動作確認

```powershell
& $whisperExe `
    -m $modelFile `
    -f ".\samples\jfk.wav"
```

### 最終的な構成

```text
C:\Users\Higashiyama\Documents\_hug-client
├─ package.json
└─ whisper.cpp
   ├─ build
   │  └─ bin
   │     └─ Release
   │        ├─ whisper-cli.exe
   │        ├─ whisper.dll
   │        ├─ ggml.dll
   │        ├─ ggml-base.dll
   │        └─ ggml-cpu.dll
   └─ models
      └─ ggml-base.bin
```

